apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: pulse-notify
  labels:
    app: audit-service
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
    - name: http
      port: 8085
      targetPort: http
