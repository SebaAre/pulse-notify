apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: pulse-notify
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 8084
      targetPort: http
