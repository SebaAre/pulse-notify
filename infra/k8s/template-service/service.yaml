apiVersion: v1
kind: Service
metadata:
  name: template-service
  namespace: pulse-notify
  labels:
    app: template-service
spec:
  type: ClusterIP
  selector:
    app: template-service
  ports:
    - name: http
      port: 8083
      targetPort: http
